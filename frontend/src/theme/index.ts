/**
 * Theme exports
 * Centralized theme configuration for the application
 */

export { theme, defaultTheme, type Theme } from './theme.config'
export { brandConfig, type BrandConfig } from './brand.config'

